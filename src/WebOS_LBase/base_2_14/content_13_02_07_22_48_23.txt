      
KLTSetVerbosity()

void KLTSetVerbosity(
int verbosity);

KLTSetVerbosity() sets the verbosity. Currently there are only two levels:
0: forbids all printing to the screen (except for error and warning messages).
1: provides feedback by printing sparse messages (the default).

